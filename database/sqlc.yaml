version: "2"
sql:
  - schema: "schema/schema.sql"
    queries: "queries/queries.sql"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "../consumer/internal/db"
        sql_package: "pgx/v5"
        emit_json_tags: true